import { Space3DigitsDirective } from './space3-digits.directive';

describe('Space3DigitsDirective', () => {
  it('should create an instance', () => {
    const directive = new Space3DigitsDirective();
    expect(directive).toBeTruthy();
  });
});
