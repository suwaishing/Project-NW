import { NumScrollDirective } from './num-scroll.directive';

describe('NumScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new NumScrollDirective();
    expect(directive).toBeTruthy();
  });
});
